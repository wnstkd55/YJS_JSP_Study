Bean (객체 설정)
	1. XML 설정 (src/main/resources) --> Spring Bean Configuration File 생성
		-- 자주 수정이 요구되는 설정
	2. 어노테이션 설정
		-- 자주 수정이 발생 되지 않는 설정
		-- Spring Bean Configuration File 에서 어노테이션을 사용할 수 있도록 설정 필요.
			-- context 네임스페이스 추가
			-- context 네임스페이스 설정
			<context:component-scan base-package="polymorphism_5_1_1">
			</context:component-scan>
		-- 클래스 선언부 윗 라인에 @Component("tv">
		-- @Componetn만 정의된 겨우 클래스 이름의 첫자를 소문자로 로드
		객체 생성 어노테이션( @Component )
			-- @Service				xxxServiceImlp	:비즈니스 로직을 처리하느 Service클래스
			-- @Repository			xxxDAO 			:DB 연동을 처리하는 객체 DAO 클래스
			-- @Controller			xxxController : 사용자 요청을 제어하는 Controller 클래스
		의존성 주입 어노테이션 (DI)
			1. @AutoWired : 주로 변수 위에 설정, 해당 타입의 객체를 찾아서 주입 (Spring)
				- 해당 타입의 객체가 여러개인 경우 @Qualifier를 사용해서 특정 이름을 지정
				
			2. @Qualifier : 특정 객체의 이름을 이용해서 의존성 주입할때 사용.(Spring)
				
			3. @Resource : @Autowired와 @Qualifier를 합친 기능 (java)
			4. @Inject : @Autowired와 동일한 기능(java)
			